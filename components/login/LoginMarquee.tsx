import { ImageSourcePropType } from "react-native";

type ImageListType = {
  imageName: string;
  imageSrc: ImageSourcePropType;
}[];

export default function LoginMarquee(imageList: ImageListType) {}
